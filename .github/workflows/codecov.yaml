#
# SPDX-License-Identifier: MIT
#
# Copyright (C) 2025 Daniel Bourdrez. All Rights Reserved.
#
# This source code is licensed under the MIT license found in the
# LICENSE file in the root directory of this source tree or at
# https://spdx.org/licenses/MIT.html
#

---

name: Codecov

on:
  workflow_call:
    inputs:
      disable_search:
        description: >-
          Disable search for coverage files. This is helpful when specifying
          what files you want to upload with the files option.
        required: false
        type: string
        default: 'true'
    secrets:
      token:
        required: false

permissions: { }

jobs:
  Codecov:
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest
    steps:
      - name: Ensure Codecov token is available
        env:
          token: ${{ secrets.token }}
        run: |
          if [ -z "$token" ]; then
            echo "Codecov token is required when running this job." >&2
            exit 1
          fi

      - name: Harden Runner
        uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76
        with:
          egress-policy: block
          allowed-endpoints: >
              github.com:443
              golang.org:443
              keybase.io:443
              objects.githubusercontent.com:443
              proxy.golang.org:443
              storage.googleapis.com:443
              sum.golang.org:443
              api.codecov.io:443
              cli.codecov.io:443
              ingest.codecov.io:443
              o26192.ingest.us.sentry.io:443

      - name: Init go
        uses: bytemare/actions/init-go@69ba525cb742a88f268ae52db7eaa9a320656a74

      - name: Code Coverage
        shell: bash
        run: |
          go test -v -race \
            -covermode=atomic \
            -coverpkg=./... \
            -coverprofile=coverage.out \
            ./...

      - uses: codecov/codecov-action@671740ac38dd9b0130fbe1cec585b89eea48d3de
        env:
          CODECOV_TOKEN: ${{ secrets.token }}
        with:
          files: coverage.out
          disable_search: ${{ inputs.disable_search }}
